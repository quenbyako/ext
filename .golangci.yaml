linters:
  enable-all: true

linters-settings:
  govet:
    enable-all: true

  varnamelen:
    ignore-decls:
      - f fmt.State

issues:
  exclude-rules:
    - path: (.+)_test.go
      linters:
        - varnamelen
        - revive
        - depguard
